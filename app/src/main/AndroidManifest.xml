<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.onlinebazar">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchAuction"></activity>
        <activity android:name=".CreateAuction" />
        <activity android:name=".UserMainActivity" />
        <activity android:name=".BuyoutOnItem" />
        <activity android:name=".BidOnItem" />
        <activity android:name=".RegisterUser_Passwd" />
        <activity android:name=".UserLogin" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".AuctionDetails" />
        <activity android:name=".ActionArrays" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>